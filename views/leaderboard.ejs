<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Minesweeper Leaderboard</title>
  <style>
    body {
      font-family: "Roboto", sans-serif;
      background-color: #121212; /* dark_background */
      padding: 20px;
      color: #E0E0E0; /* text_primary */
      margin: 0;
    }
    
    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }
    
    h1 {
      text-align: center;
      margin-bottom: 30px;
      color: #6200EE; /* accent_purple */
      font-size: 36px;
      font-weight: bold;
      text-shadow: 0 2px 4px rgba(0,0,0,0.3);
    }
    
    .subtitle {
      text-align: center;
      color: #A0A0A0; /* text_secondary */
      margin-bottom: 30px;
      font-size: 18px;
    }
    
    table {
      width: 100%;
      border-collapse: collapse;
      background-color: #1E1E1E; /* surface_dark */
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 20px rgba(0,0,0,0.3);
      margin-bottom: 30px;
    }
    
    th, td {
      padding: 18px;
      text-align: center;
      border-bottom: 1px solid #2A2A2A; /* surface_light */
    }
    
    thead {
      background-color: #6200EE; /* primary_purple */
      color: white;
      font-size: 18px;
      font-weight: 500;
    }
    
    tr:nth-child(even) {
      background-color: #2A2A2A; /* surface_light */
    }
    
    
    .rank-1 {
      color: #FFD700; /* gold for 1st place */
      font-weight: bold;
    }
    
    .rank-2 {
      color: #C0C0C0; /* silver for 2nd place */
    }
    
    .rank-3 {
      color: #CD7F32; /* bronze for 3rd place */
    }
    
    .gems-cell {
      color: #00C853; /* gems_color */
      font-weight: bold;
    }
    
    .user-cell {
      font-weight: 500;
    }
    
    .footer {
      text-align: center;
      color: #A0A0A0; /* text_secondary */
      margin-top: 30px;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>💎 Minesweeper Leaderboard</h1>
    <div class="subtitle">Top players by gem collection</div>
    
    <table>
      <thead>
        <tr>
          <th>Rank</th>
          <th>Username</th>
          <th>Gems</th>
        </tr>
      </thead>
      <tbody>
        <% users.forEach((user, index) => { %>
          <tr>
            <td class="rank-<%= index < 3 ? index+1 : '' %>">
              <%= index + 1 %>
              <% if(index < 3) { %>
                <span style="font-size:20px">
                  <%= index === 0 ? '🥇' : index === 1 ? '🥈' : '🥉' %>
                </span>
              <% } %>
            </td>
            <td class="user-cell"><%= user.username %></td>
            <td class="gems-cell"><%= user.gems.toFixed(2) %> 💎</td>
          </tr>
        <% }) %>
      </tbody>
    </table>
    
    <div class="footer">
      Last updated: <%= new Date().toLocaleString() %>
    </div>
  </div>
</body>
</html>